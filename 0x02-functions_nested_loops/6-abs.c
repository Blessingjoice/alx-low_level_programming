#include "main.h"
/**
 * _abs - calculate the absolute value
 * of an integer.
 *
 * @n : input number as an integer.
 * Return: absolute value.
 */
int _abs(int n)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
